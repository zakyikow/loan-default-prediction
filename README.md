# loan-default-prediction
Predict whether borrowers will default on loans using tabular financial data. Build a custom MLP with L1/L2 regularization and dropout, compare against logistic regression (with Elastic Net) and Random Forest.
